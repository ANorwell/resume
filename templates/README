These files are template files used to generate resumes using the
resume.pl script. They are not intended to be interacted with
directly; use the makefile in the parent directory to generate
resumes.

Latex setup and run:

  tlmgr init-usertree
  sudo apt-get install texlive-latex-extra texlive-latex-recommended xzdec
  sudo tlmgr option repository ftp://tug.org/historic/systems/texlive/2017/tlnet-final
  tlmgr install moderncv etoolbox
  pdflatex mcv.tex
